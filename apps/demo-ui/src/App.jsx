/**
 * Root React app for the FinContext demo console.
 *
 * This first scaffold is intentionally tiny. Later commits add the analyst
 * console shell, backend client, tabs, and sample-data fallback.
 */
export default function App() {
  return (
    <main className="app-shell">
      <h1>FinContext Agent</h1>
      <p>SEC disclosure drift research console.</p>
    </main>
  );
}
